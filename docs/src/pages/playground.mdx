import ConnectButton from "../jacdac/ConnectButton"
import JacdacComponent from "../jacdac/Component"
import JacDaciQL from "../jacdac/GraphiQL"
const query = `
{
    devices {
        id
        connected
        services {
            id
            name
        }
    }
}
`

# Hello

A connect button

<ConnectButton />

A graphiql interctive eelemnt

<JacDaciQL query={query}>
</JacDaciQL>
