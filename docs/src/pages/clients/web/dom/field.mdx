import DeviceList from "../../../../components/DeviceList"

# JDRegister

> A **field** is a [DOM](/clients/web/dom) [node](/clients/web/dom/node) that represents a data field in a [register](/clients/web/dom/register). 

<DeviceList />

This object inherits methods and properties from [node](/clients/web/dom/node).

## Methods

### ``name``

The name of the field as defined in the specification.

```typescript
const name = field.name
```

### ``unit``

The unit of the value as defined in the service speciciation.

```typescript
const unit = field.unit
```

### ``value``

The value of the field received in the latest package. If no data has been received, undefined.

```typescript
const v = field.value
```