---
deck: true
title: JACDAC
---

import { serviceSpecificationFromName, deviceSpecificationFromFirmwareIdentifier } from "../../../../src/jdom/spec"
import { serviceSpecificationToDTDL } from "../../../../src/azure-iot/dtdl"

# JACDAC

"USB" for low-cost micro-controllers

[https://aka.ms/jacdac](https://aka.ms/jacdac)

# "USB" for low-cost micro-controllers

* Dynamic Connectivity
* Dynamic Device Discovery
* Hardware abstraction

# Bus, Devices, Services

![JACDAC stack](../../../../jacdac-spec/spec/images/jacdac-stack.png)

# Service specification

Hardware abstraction: registers, events, commands.

<pre>
    <code className="language-markdown">{serviceSpecificationFromName("slider").source}</code>
</pre>

# Services Catalog

Discover registers, events, and commands on the fly.

<ServiceSpecificationList count={3} shuffle={true} />

# Service Twins

Repository of DTDL models for the services ([more...](/dtmi)).

<pre>
    <code className="language-json">{JSON.stringify(serviceSpecificationToDTDL(serviceSpecificationFromName("slider")), null, 2)}</code>
</pre>

# Device specification

Define metadata, services and firmware identifiers.

<pre>
    <code className="language-json">{JSON.stringify(deviceSpecificationFromFirmwareIdentifier(0x30838b8e), null, 2)}</code>
</pre>

# Device catalog

Identify module, discover metadata and firmware updates.

<DeviceSpecificationList count={4} shuffle={true} />

# Open Source Catalog

* Service list in [JACDAC repo](https://github.com/microsoft/jacdac) 
* Device list in [JACDAC repo](https://github.com/microsoft/jacdac) 
* Vendor-specific repos for device firmware releases

# Web Tools

Live JDOM exploration, data streaming, tracing in your browser via WebUSB (WebBLE?)

<PacketsPreview />

# [Firmware updates](/tools/updater) 

* Sniff firmware identifier
    * Query Devices Catalog
        * Check GitHub Release
            * Download firmware
                * Deploy firmware over JACDAC

# Physical layer

![An image depicting a JACDAC transmission. A JACDAC transmission begins with a start pulse of 11-14 us, a inter lo data spacing of minimally 40 us before data commences. A JACDAC transmission ends with an end pulse of 11-14 us.](../../../../jacdac-spec/spec/images/jacdac-activity.png)

# Frames

| Memory offset | Field size (bytes) 	| Field name 	| Description  	|
|--------|------------	|-------------	|----------	|
| 0 | 2          	|`frame_crc`	| [16-bit CRC CCITT](https://en.wikipedia.org/wiki/Cyclic_redundancy_check) of all following fields. |
| 2 | 1          	|`frame_size`	| Size of the data field.	|
| 3 | 1          	|`frame_flags`	| Flags specific to this frame.	|
| 4 | 8          	|`device_identifier`	| 64-bit device identifier.	|
| 12 | 1 * `frame_size`   	|`data`	| The data payload of the frame.	|

# Packets

| Memory offset | Field size (bytes) 	| Field name 	| Description  	|
|--------|------------	|-------------	|----------	|
| 12 | 1	|`packet_size`	| The size of the payload field. Maximum size is 236 bytes. |
| 13 | 1	|`service_instance`	| The instance number of the destination service.	|
| 14 | 2	|`service_command`	| A number that specifies an operation and code combination.	|
| 16 | 1 * packet_size	|`payload`	| The packet data payload destined for a particular service.	|