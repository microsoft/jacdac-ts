import DeviceList from "../components/DeviceList"
import { Link } from "gatsby"

<h1>{props.pageContext.node.name} service</h1>

<ul>
    <li>class identifier: 0x{props.pageContext.node.classIdentifier.toString(16)}</li>
</ul>

## Playground

List of devices with this service.

<DeviceList serviceClass={props.pageContext.node.classIdentifier} />

## Specification

<pre>
    {props.pageContext.node.source}
</pre>


## See Also

<ul>
    <li><a href={`https://microsoft.github.io/jacdac/services/${props.pageContext.node.shortId}`}>specification</a>.</li>
    <li>Using services in JavaScript with <Link to={`/clients/web/dom/service`}>JDService</Link></li>
</ul>

